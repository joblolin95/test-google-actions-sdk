Install Node.js

Create Google Actions project
* Take note of project id

$ npm install -g firebase-tools

$ firebase login

$ firebase list

$ firebase init

$ npm install actions-on-google

$ firebase use <project id>

$ firebase deploy --only functions

$ ./gactions test --action_package action.json --preview_mins 1234 -project [Google Actions project id]
